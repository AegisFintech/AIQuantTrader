# M2.3d XAU Parity Report

## Status

M2.3d adds the remaining BTC SMC gate wrapper, the SMC-score-aware
`DailyRiskSizer`, and the XAU parity tests for Issue #22.

PR: #28 from branch `m23/xau-btc-smc-sizing-and-parity`.

## Live Ack Run

- Ack file: `.runtime/wineprefix/drive_c/users/root/AppData/Roaming/MetaQuotes/Terminal/Common/Files/finrobot_acks.csv`
- Current XAU `AUTO_FILLED` acks in the live file: 26 from `2026.06.11 06:30:14` through `2026.06.12 04:43:17`.
- June 11 XAU filled acks: 19.
- DuckDB source rows for XAU:
  - `tsv:/root/FinRobot/data/XAUUSD1.csv`: 100,000 non-null OHLC rows, ending `2026-05-01 02:59:00` server time.
  - `status_snapshot`: 2 June 11 rows, both without OHLC values.
- Eligible live ack/bar overlap for the parity test: 0 filled acks.
- Live test result: `tests/test_xau_parity_live.py` is xfailed until June 11+ non-null XAU OHLC bars are exported.

No live mismatches are available yet because there are no non-null June 11 OHLC bars to compare against the live acks. The test filters to ack decisions that have matching bars, then enforces the issue target of `>=95%` match rate on that overlap.

## May 2026 Harness Self-Test

- Bars: 180 real XAU M1 bars from `2026-05-01 00:00:00` through `2026-05-01 02:59:00` server time.
- Synthetic acks: generated from the XAU gated strategy and replayed through the same parity harness.
- Result: 8/8 matched, `100.00%`.
- Mismatches: none.

This self-test validates the parity pipeline on real M1 data. A real `>=95%` live match at scale still requires MT5 to export the missing June 11+ OHLC bars.

## Rerun Procedure

```bash
python3 scripts/mt5_trade_report.py
.venv/bin/pytest -s tests/test_xau_parity_live.py tests/test_xau_parity_may_self_test.py
```

When the export pipeline has written non-null XAU bars covering the live acks, `tests/test_xau_parity_live.py` should stop xfailed execution and report the live overlap match rate. With only two eligible acks, the acceptance result must be 2/2 to satisfy the `>=95%` target.

## Closure Note

M2.3d shipped via PR #28. Issue #22 is marked complete; M2.4
per-strategy attribution and reporting is next.
