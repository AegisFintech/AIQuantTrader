schema_version = 1
policy_id = "legacy-archive-credential-scan-v1"
recursive_archive_scan = true
maximum_findings = 0
required_detectors = [
  "api_token",
  "password",
  "private_key",
  "seed_phrase",
  "session_credential",
]
